project(TEST)

set(SRC_LIST src/test/main.cpp)

message(STATUS "This is BINARY dir " ${TEST_BINARY_DIR})
message(STATUS "This is SOURCE dir " ${TEST_SOURCE_DIR})

add_executable(test ${SRC_LIST})

add_subdirectory(src bin)
